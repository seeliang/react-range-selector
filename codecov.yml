coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:  
      default:
        enabled: yes
        target: 90%
    patch:  
      default:
        enabled: yes
        target: 70%

comment:
  layout: "reach, diff, flags, files, footer"
  behavior: default